import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'To-do-vezbe1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
